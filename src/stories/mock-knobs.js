const text = (description, val) => val
const boolean = (description, val) => val
const select = (description, val, defaultVal) => defaultVal
const number = (description, val) => val
const array = (description, val) => val
const widthKnobs = (description, val) => val
const date = (description, val) => val

export { text, boolean, select, number, array, widthKnobs, date }
