import Vuex from 'vuex'

const state = {

}

const mutations = {

}

export default new Vuex.Store({
  strict: process.env.NODE_ENV !== 'production',
  state,
  actions: {},
  mutations: mutations,
  modules: {}
})
